---
const { title, description, link } = Astro.props;
---
<div class="bg-white text-gray-900 rounded-xl p-6 shadow-lg transition-transform duration-300 hover:scale-105">
  <h3 class="text-lg font-bold">{title}</h3>
  <p class="text-sm">{description}</p>
  <a href={link} class="text-blue-600 underline mt-2 block">Ver proyecto</a>
</div>
